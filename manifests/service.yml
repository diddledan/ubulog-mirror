apiVersion: v1
kind: Service
metadata:
    name: "danik8s-d1e8"
    labels:
        app: "danik8s-d1e8"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "danik8s-d1e8"